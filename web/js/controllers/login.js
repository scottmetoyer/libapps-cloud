(function () {
  // ===============================================================================
  // Controllers / Login
  //

  function LoginCtrl($http, $state, $filter, bl, data, Auth) {

  }

  angular.module('pixeladmin')
    .controller('LoginCtrl', LoginCtrl);
})();